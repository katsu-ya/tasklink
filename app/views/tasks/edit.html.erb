<turbo-frame id="modal">
  <h2 class="text-lg font-bold mb-4">タスク編集</h2>

  <%= render "form", task: @task %>

  <button onclick="closeModal()"
          class="mt-4 text-gray-500">
    閉じる
  </button>
</turbo-frame>

<h1>タスク一覧</h1>

<%= link_to "新規作成", new_task_path %>

<% @tasks.each do |task| %>
  <div>
    <h2><%= task.title %></h2>
    <p><%= task.description %></p>
    <small><%= task.user.email %></small>
  </div>
<% end %>